<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuecheng.content.mapper.TeachplanMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.xuecheng.content.model.po.Teachplan">
        <id column="id" property="id" />
        <result column="pname" property="pname" />
        <result column="parentid" property="parentid" />
        <result column="grade" property="grade" />
        <result column="media_type" property="mediaType" />
        <result column="start_time" property="startTime" />
        <result column="end_time" property="endTime" />
        <result column="description" property="description" />
        <result column="timelength" property="timelength" />
        <result column="orderby" property="orderby" />
        <result column="course_id" property="courseId" />
        <result column="course_pub_id" property="coursePubId" />
        <result column="status" property="status" />
        <result column="is_preview" property="isPreview" />
        <result column="create_date" property="createDate" />
        <result column="change_date" property="changeDate" />
    </resultMap>



    <resultMap id="treeNodeResultMap" type="com.xuecheng.content.model.vo.TeachplanVo">
        <id     column="one_id"        property="id" />
        <result column="one_pname"      property="pname" />
        <result column="one_parentid"     property="parentid" />
        <result column="one_grade"  property="grade" />
        <result column="one_mediaType"   property="mediaType" />
        <result column="one_stratTime"   property="stratTime" />
        <result column="one_endTime"   property="endTime" />
        <result column="one_orderby"   property="orderby" />
        <result column="one_courseId"   property="courseId" />
        <result column="one_coursePubId"   property="coursePubId" />

        <collection property="teachPlanTreeNodes" ofType="com.xuecheng.content.model.vo.TeachplanVo">
            <!-- 二级数据映射 -->
            <id     column="two_id"        property="id" />
            <result column="two_pname"      property="pname" />
            <result column="two_parentid"     property="parentid" />
            <result column="two_grade"  property="grade" />
            <result column="two_mediaType"   property="mediaType" />
            <result column="two_stratTime"   property="stratTime" />
            <result column="two_endTime"   property="endTime" />
            <result column="two_orderby"   property="orderby" />
            <result column="two_courseId"   property="courseId" />
            <result column="two_coursePubId"   property="coursePubId" />
            <association property="teachplanMedia" javaType="com.xuecheng.content.model.po.TeachplanMedia">
                <result column="teachplanMeidaId"   property="id" />
                <result column="mediaFilename"   property="mediaFilename" />
                <result column="mediaId"   property="mediaId" />
            </association>

        </collection>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, pname, parentid, grade, media_type, start_time, end_time, description, timelength, orderby, course_id, course_pub_id, status, is_preview, create_date, change_date
    </sql>

    <select id="selectTeachPlanTree" resultMap="treeNodeResultMap" parameterType="long">
        select
            t1.id             one_id,
            t1.pname          one_pname,
            t1.parentid       one_parentid,
            t1.grade          one_grade,
            t1.media_type     one_mediaType,
            t1.start_time     one_stratTime,
            t1.end_time       one_endTime,
            t1.orderby        one_orderby,
            t1.course_id      one_courseId,
            t1.course_pub_id  one_coursePubId,
            t2.id             two_id,
            t2.pname          two_pname,
            t2.parentid       two_parentid,
            t2.grade          two_grade,
            t2.media_type     two_mediaType,
            t2.start_time     two_stratTime,
            t2.end_time       two_endTime,
            t2.orderby        two_orderby,
            t2.course_id      two_courseId,
            t2.course_pub_id  two_coursePubId,
            tm.media_fileName mediaFilename,
            tm.id teachplanMeidaId,
            tm.media_id mediaId

        from teachplan t1
                 left join teachplan t2
                            on t2.parentid=t1.id
                 left join teachplan_media tm
                           on tm.teachplan_id=t2.id
        where t1.parentid=0 and t1.course_id=#{courseId}
        order by t1.orderby,t2.orderby
    </select>

    <select id="selectMaxOrderFromTeachplanByCourseIdAndParentId" resultType="int">
        select max(orderby) from teachplan where parentid=#{parentId} and course_id=#{courseId}
    </select>

    <select id="selectMinOrderFromTeachplanByCourseIdAndParentId" resultType="int">
        select min(orderby) from teachplan where parentid=#{parentId} and course_id=#{courseId}
    </select>

    <select id="selectChildTeachPlanByParentId" parameterType="long" resultType="com.xuecheng.content.model.po.Teachplan">
        select <include refid="Base_Column_List"></include>
        from teachplan
        where parentid=#{teachplanId}
    </select>

    <select id="selectUpTeachplan" resultType="com.xuecheng.content.model.po.Teachplan">
        select <include refid="Base_Column_List"></include>
        from teachplan
        where course_id=#{courseId} and parentId=#{parentId} and orderby &lt; #{orderBy}
    </select>

    <select id="selectDownTeachplan" resultType="com.xuecheng.content.model.po.Teachplan">
        select <include refid="Base_Column_List"></include>
        from teachplan
        where course_id=#{courseId} and parentId=#{parentId} and orderby &gt; #{orderBy}
    </select>


</mapper>
